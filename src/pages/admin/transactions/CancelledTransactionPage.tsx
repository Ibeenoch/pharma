import React from "react";
import CancelledTransaction from "../../../features/admin/transaction/CancelledTransaction";

const CancelledTransactionPage = React.memo(() => {
  return <CancelledTransaction />;
});

export default CancelledTransactionPage;
