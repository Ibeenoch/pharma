import React from "react";
import CompletedTransaction from "../../../features/admin/transaction/CompletedTransaction";

const CompletedTransactionPage = React.memo(() => {
  return <CompletedTransaction />;
});

export default CompletedTransactionPage;
