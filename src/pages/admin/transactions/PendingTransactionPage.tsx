import React from "react";
import PendingTransaction from "../../../features/admin/transaction/PendingTransaction";

const PendingTransactionPage = React.memo(() => {
  return <PendingTransaction />;
});

export default PendingTransactionPage;
